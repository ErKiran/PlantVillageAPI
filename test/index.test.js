/*const Unst = require('../models/unstructured');
const assert = require('assert');


describe('Test for the data on database', () => {
    it('should include 37 different lables', async () => {
        const res = await Unst.find({});
        assert.equal(res.length, 37)
    })
})*/